# 数据预处理前三步详解

本文档整理了 SceneFactor README 中数据预处理前三步（`store_manifold_3dfuture.py`、`fix_3dfuture_meshes.py`、`store_3dfuturefront.py`）的详细说明，包含相关数据集（3D-FUTURE、3D-FRONT）和工具（Manifold）的背景知识。

---

## 一、前三步概览

| 步骤 | 脚本 | 目的 | 类比 |
|------|------|------|------|
| 1 | `store_manifold_3dfuture.py` | 修复 3D-FUTURE 家具网格的非流形问题 | 把破损的零件修好 |
| 2 | `fix_3dfuture_meshes.py`（可选但推荐） | 简化网格、填补残漏洞 | 给零件抛光、补漆 |
| 3 | `store_3dfuturefront.py` | 把家具按布局装进房间，组装完整场景 | 按图纸把家具摆好 |

三步完成后，产出的就是**完整、干净、带场景布局信息的 3D 场景网格文件**，作为后续 SDF 体素计算和训练块采样的基础。

---

## 二、3D-FUTURE 数据集

**3D-FUTURE**（3D Furniture shape with TextURE）是**阿里巴巴**（淘宝技术部）在 2020 年开源的大型 3D 家具数据集。论文：[arXiv:2009.09633](https://arxiv.org/abs/2009.09633)

### 核心规模

- **11,625** 个独特的 3D 家具模型（含纹理）
- 覆盖 **39** 个家具类别（床、沙发、桌子、椅子、柜子等）
- 每个模型附带：`raw_model.obj`（原始 CAD 模型）、`normalized_model.obj`（归一化版本）、高分辨率纹理贴图（`texture.png`）、材质文件（`.mtl`）

### 它为什么知名

1. **工业级质量**——这些模型来自阿里巴巴"躺平设计家"平台的真实 CAD 资产，是实际生产中使用的模型，而不是网上低质量抓取。相比之下，以前很多学术数据集（如 ShapeNet）的模型纹理粗糙或缺失。
2. **高分辨率纹理**——每个模型都有专业设计师制作的颜色/材质纹理，对"纹理恢复""材质识别"等任务极其重要。
3. **丰富的标注**——每个模型带有风格（欧式、现代等）、主题、材料、类别等细粒度属性标签。
4. **2D-3D 严格对齐**——数据集中提供的渲染图像与 3D 模型是完全对应的（不是伪匹配），可支持实例分割、6-DoF 姿态估计等任务。

### 学术地位

3D-FUTURE 是**室内场景理解和生成领域的主流数据集之一**，常与 3D-FRONT 配对使用。许可证为 **CC-BY-SA-NC 4.0**（非商业用途）。

---

## 三、3D-FRONT 数据集

**3D-FRONT**（3D Furnished Rooms with layOuts and semaNTics）是和 3D-FUTURE 配套的**室内场景布局数据集**，发表于 ICCV 2021。论文：[arXiv:2011.09127](https://arxiv.org/abs/2011.09127)

### 核心规模

- **6,813** 套房屋 / **18,968** 个精装修房间
- **31** 种房间类型（卧室、客厅、餐厅、厨房等）
- 场景由**专业室内设计师**设计布局

### 与其他数据集的对比

| 数据集 | 房间数 | 特点 |
|--------|--------|------|
| ScanNet | 1,506 | 真实扫描，有噪声 |
| Matterport3D | 2,056 | 真实扫描 |
| 3D-FRONT | **18,968** | 专业设计师布局，高质量纹理 |

3D-FRONT 是目前最大的**合成室内场景**数据集之一。

### 两者关系

**3D-FUTURE = 家具零件库**（11,000+ 个带纹理的家具 3D 模型）

**3D-FRONT = 装修图纸集合**（18,000+ 个房间，每间告诉你"什么家具摆在哪里"）

---

## 四、第 1 步详解：store_manifold_3dfuture.py

### 做什么

对每个 3D-FUTURE 的家具网格调用 [hjwdzh/Manifold](https://github.com/hjwdzh/Manifold) 工具，修复非流形网格。

### 输入

原始 3D-FUTURE 数据集中的家具 3D 模型（`.obj` 网格文件）

### 为什么需要这一步

- 3D-FUTURE 数据集里很多家具模型存在拓扑瑕疵（非流形问题）
- 后续计算 SDF（有符号距离场）要求输入必须是**干净的流形网格（watertight manifold）**，否则 SDF 会出错
- 不做这一步，后面的所有训练都无法进行

---

## 五、第 2 步详解：fix_3dfuture_meshes.py

### 做什么

- **简化网格**（mesh simplification）：减少过于密集的三角面数量，降低复杂度
- **填充孔洞**（hole filling）：第 1 步处理后仍可能存在的少量孔洞在这一步补全

### 输入

第 1 步修复后的网格

### 为什么需要这一步

- 第 1 步只修复了拓扑问题（非流形），但并没有对网格做简化和孔洞填充
- 有些模型的网格过于精细（几十万面片），训练时会产生不必要的计算开销
- 第 1 步完成后仍可能存在少量孔洞，这一步进一步补全

### 注意

这个步骤标记为"可选但推荐"——有些模型不需要这一步也能正常工作，但做了能提升整体数据质量。

---

## 六、第 3 步详解：store_3dfuturefront.py

### 做什么

- **组装场景**：读取 3D-FRONT 的 JSON 布局文件，把每个房间中摆放的家具（来自 3D-FUTURE）按照指定的位置、旋转、缩放放到场景中
- **存储最终网格**：把每个完整场景的几何体（所有家具 + 房间结构）合并存储为一个文件

### 输入

- 第 1-2 步处理好的 3D-FUTURE 家具模型
- 3D-FRONT 数据集的**场景布局信息**（房间结构、家具的摆放位置/朝向）

### 关键优化（README 中特别说明）

- 原始代码会把每个场景中重复出现的家具（比如同一把椅子在多个房间出现）的网格数据重复存储，导致磁盘占用极大，加载时也需要大量内存
- **优化后**：只存储每个家具模型的**变换矩阵**（位置、旋转、缩放），而不是重复存储网格本身。加载时动态计算每个实例的最终几何

这是本项目的一个实际贡献，大幅降低了存储和内存需求。

---

## 七、Manifold 是什么

### hjwdzh/Manifold（SceneFactor 使用的）

这个 Manifold 是 [hjwdzh/Manifold](https://github.com/hjwdzh/Manifold)（黄、苏、Guibas 2018 年的工作），一个**专门把任意三角形网格转成 watertight manifold 网格**的工具。

核心思路是用**八叉树（octree）**方法：把输入网格体素化，然后从体素中提取一个保证是流形的表面。

```bash
./manifold input.obj output.obj [分辨率]
```

它还有一个增强版 [ManifoldPlus](https://github.com/hjwdzh/ManifoldPlus)（2020），能更好地处理更复杂的输入。

### 容易混淆的 elalish/manifold

现在 3D 生成领域更常提到的是 [elalish/manifold](https://github.com/elalish/manifold)——Google 开源的 **C++ 几何库**，功能强大得多：
- 做 **CSG 布尔运算**（union, subtract, intersect）
- 保证所有操作结果都是流形
- 常被用作 3D 生成模型的后处理

### 两者对比

| | hjwdzh/Manifold（SceneFactor 用的） | elalish/manifold |
|---|---|---|
| 目的 | 修拓扑，保证流形+watertight | 做布尔运算，保证流形 |
| 输出 | 三角形网格（质量一般） | 三角形网格（质量好） |
| 是否生成模型常用后处理 | 有时用 | 经常用 |

---

## 八、流形网格 vs 非流形网格

### 流形网格（manifold mesh）

表面上每个点的邻域都像一个**小圆盘**（可以展开成平面）。具体地说：
- 每条边被**恰好两个面**共享
- 每个顶点周围的面能排成一个闭合的环

### 非流形网格（non-manifold mesh）

破坏了上述性质的网格。常见情况：

| 类型 | 图示想象 | 问题 |
|------|----------|------|
| 共享边超过两个面 | 三张纸共用一条边，呈"Y"形 | 这条边属于三个面 |
| 悬挂边/面 | 一个面片边缘拖着一根线 | 边只被一个面共享 |
| T 型连接 | 一个顶点落在另一条边的中间 | 破坏了边的二面共享规则 |
| 孤立顶点 | 一个点悬浮，不连任何面 | 无用数据 |
| 法向不一致 | 相邻面的法线指向相反方向 | 无法区分内外 |

### 一个关键区别

- **Watertight**（封闭无漏孔）：能装水不漏
- **Manifold**（流形）：每条边恰好两个面共享
- 两者不是同一回事——一张平铺的纸是流形的但不是 watertight

SceneFactor 的要求是**既是流形又是 watertight**，因为后续计算 SDF 要求每个点能明确判断是在物体内部还是外部。

---

## 九、专业 CAD 模型为何会有非流形问题？

这恰恰是因为它们是专业设计师用 CAD 工具制作的。

### 原因一：CAD 建模的工作流天然产生非流形

专业室内设计师用 3ds Max、SketchUp、Blender 等工具做**显式建模**，本质上是不断做布尔运算、拉伸、镜像、合并。这些操作极易产生非流形结构：

```
设计师的操作：                    网格中实际发生的事：
- 两张桌子镜像对称摆放    →   两个网格在对称轴共用一条边 → non-manifold edge
- 柜门和柜体分别建模再合并 →   门轴处出现多面共享边       → non-manifold edge  
- 在墙上掏一个窗户        →   Boolean 操作产生退化三角形   → 孔洞附近拓扑混乱
- 拉伸一个面到另一个面内部 →   出现内部面（interior face）  → 内外不明确
```

### 原因二：工业资产的目标是视觉效果，不是拓扑正确性

3D-FUTURE 的模型来自"躺平设计家"平台真实电商场景，设计师关注的是**渲染效果**，不是每条边是不是恰好两个面共享。模型可能包含多层结构、螺栓孔位等工程细节，这些对渲染无关但对拓扑是破坏。

### 原因三：格式转换也会引入非流形

不同 CAD 软件之间的格式转换（例如 `.max` 导出到 `.obj`）过程中，精度舍入、坐标变换、焊接顶点等操作会引入各种微小的拓扑缺陷。

### 打个比方

就像专业的建筑设计图纸——设计流畅、视觉效果完美，但如果严格检查每一条线的端点是否精确连接、每一个交点的连接数是否严格等于 2，你会发现大量"瑕疵"。这些瑕疵在"看"的时候完全没问题，但要用 CAD 做结构分析或者 3D 打印时就必须先清理。

**3D 网格同理**：作为渲染素材，这些模型完全合格；但要拿去算 SDF（需要明确的内部/外部区分），就必须把拓扑修到严格的流形标准——这就是 Manifold 工具做的事。

---

## 十、和其他网格后处理工具的区别

| 任务 | 工具举例 | 做什么 |
|------|----------|--------|
| **Retopology（重拓扑）** | ZRemesher, QuadRemesh | 把三角面重建成**四边形**，布线规整，方便动画/细分 |
| **Remeshing（重网格化）** | Instant Meshes, Botsch et al. | 重新布三角形，让面大小均匀、形状规整 |
| **Simplification（简化）** | QEM, MeshLab 的 decimation | 减少面数，保留主要形状 |
| **Manifold 修复** | **hjwdzh/Manifold** | **修复拓扑，保证流形和 watertight** |

Manifold 只关心最后一项，不关心生成的是不是规整的三角形或四边形。

---

Sources:
- [3D-FUTURE 论文](https://arxiv.org/abs/2009.09633)
- [3D-FRONT 论文](https://arxiv.org/abs/2011.09127)
- [3D-FUTURE 阿里云天池页面](https://tianchi.aliyun.com/dataset/98063)
- [hjwdzh/Manifold](https://github.com/hjwdzh/Manifold)
- [elalish/manifold](https://github.com/elalish/manifold)
