# SceneFactor 项目复现阶段 — 任务安排（李嘉睿）

## 📋 整体定位

- **项目**: SceneFactor — 扩散模型 3D 场景生成（factored diffusion: semantic + geometric）
- **阶段**: 项目复现阶段，数据预处理 Steps 1-3 已完成
- **当前目标**: 完成 **Step 4** — SDF 体素计算
- **工作流**: 本地修改/小数据集验证 → GitHub 上传 → 远程工作站/全量数据集验证
- **核心代码**: `data_processing/compute_sdf_3dfront_hjm.py`（团队优化版）

---

## 🗺️ 完整数据流全景

```
Steps 1-3 (已完成)
  └→ SELECTED_FRONT_SCENES/<scene_id>/
       ├── scene.obj              (场景墙壁/地板网格)
       ├── furniture_jids.json    (家具 jid 列表)
       ├── furniture_points.json  (家具点云 + 语义标签)
       ├── scene_points.json      (场景点云)
       ├── *_trans.npy            (家具变换矩阵)
       └── *_furniture.obj        (Cabinet 等特殊家具)

Step 4 ← 你当前的任务
  └→ chunked_data_lowres/<scene_id>/
       ├── {x}_0_{z}.npy          (SDF 张量, 90×90×90, float16)
       ├── {x}_0_{z}_semantic.npy (语义 ID, 90×90×90, int16)
       ├── {x}_0_{z}_instance.npy (实例 ID, 90×90×90, int16)
       ├── {x}_0_{z}_canonic.npy  (正则坐标, 90×90×90×3, int16)
       └── {x}_0_{z}.json         (元数据)

Step 5 → 训练 subchunk 采样
Step 6 → 推理 chunk 生成
Steps 7-9 → Caption 生成
Training Stages 1-4 → VQ-VAE + Diffusion 训练
```

---

## ✅ 任务清单

### 任务 1: GitHub 协作配置
- [ ] 把 GitHub 用户名/注册邮箱发给导师
- [ ] 配置 SSH Key 并添加到 GitHub
- [ ] Clone 项目并确认可 push

### 任务 2: 本地环境部署
- [ ] `conda env create -f environment.yml`
- [ ] `pip install mesh2sdf transformers einops rotary-embedding-torch prettytable`
- [ ] 验证: `python -c "import torch; import trimesh; import mesh2sdf; print('OK')"`
- ⚠️ 你本地 CUDA 12.4 / torch 2.12.0 与原版 CUDA 11.7 不同，环境可能需适配

### 任务 3: 理解 Step 4 代码（重点）
**关键文件**: `data_processing/compute_sdf_3dfront_hjm.py`

**核心逻辑**:
```
遍历场景 → 加载 scene.obj + 所有家具 mesh（按jid加载）
         → 按 3.8m 间距切分 chunk
         → 对每个 chunk: mesh2sdf → 截断 → cKDTree 语义映射 → 保存
```

**已知内存瓶颈**:
- 大场景所有家具 mesh 同时加载（`load_scene_furniture` → `trimesh.util.concatenate`）
- line 144 的跳过条件有 bug（`all_mesh_furniture` 在判断时未定义）
- 只处理 `y=0` 层的 chunk

### 任务 4: 内存优化（如遇 OOM 再做）
三级优化:
1. **🥇 低成本**: 细化 try-catch、加 `del` + `gc.collect()`、修复 line 144 bug
2. **🥈 中等**: 大场景分 chunk 组处理，避免全部家具同时驻留
3. **🥉 较大**: 按 chunk 空间范围过滤家具（加载前先判断位置）

### 任务 5: 小数据集本地验证
- [ ] 拿到导师推送的数据集子集后
- [ ] 配置 `compute_sdf_3dfront_hjm.py` 中的路径
- [ ] 单场景运行: `python compute_sdf_3dfront_hjm.py -n 1 -p 0`
- [ ] 用 `check_chunks.py` 检查输出完整性

### 任务 6: 远程工作站配置与全量运行
- [ ] 获取工作站 SSH 信息
- [ ] SSH key 认证 + 环境部署
- [ ] 多进程全量运行: `-n 4 -p 0/1/2/3`
- [ ] 用 tmux/screen 保持后台

### 任务 7: GitHub 工作流
```
本地开发 → git commit → git push → 工作站 git pull → 全量验证
```

### 任务 8: 知识沉淀
- [ ] 运行日志和问题记录
- [ ] 踩坑记录（方便团队其他人）
- [ ] 后续 Steps 5-9 的准备

---

## 📊 分阶段时间安排

### 第一阶段：环境准备（拿到 GitHub 权限后 1-2 天）
重点: 任务 1 → 任务 2 → 任务 3

先别急着跑代码，花一天把代码结构和数据流理解清楚。你的学习方式是"先建框架再实战"，这一阶段对你来说是最重要的地基。

**具体产出**: 能向团队讲清楚 Step 4 的输入是什么、输出是什么、每一步在做什么。

### 第二阶段：本地验证（拿到数据集后 1-2 天）
重点: 任务 5（必要时先做任务 4）

用 1-2 个场景验证流程，确保输出格式正确。

**具体产出**: 本地成功跑通 1 个场景，`chunked_data_lowres` 下有完整的 SDF/semantic/instance 文件。

### 第三阶段：全量运行（1-2 天）
重点: 任务 6 + 任务 7

在工作站上部署环境、同步代码、全量运行。一边跑一边在 GitHub 上更新进度。

**具体产出**: 所有 valid_scenes 的 chunk 数据生成完毕，GitHub 上有你的提交记录。

### 第四阶段：复盘与沉淀（0.5 天）
重点: 任务 8

把这段时间的所有踩坑和经验整理成文档，记到你的 Obsidian 库里。

---

## 💡 对你个人节奏的建议（基于你的档案）

| 你的特点 | 对应策略 |
|---------|---------|
| 爆发型工作节奏 | 集中 2-3 天攻一个里程碑，中间留缓冲 |
| 先框架再实战 | 先理解全景（任务 3 是核心），再动手改代码 |
| 代码能力待提高 | 认真做版本管理、写注释，这是练工程的好机会 |
| 持续性差 | 每完成一步就 push + 写日记，让进度可视化 |
| 意义感驱动 | 记住: 这个项目是你进入具身智能科研的实战入口 |

---

## 🔗 关键文件索引

| 文件 | 作用 |
|------|------|
| `data_processing/compute_sdf_3dfront_hjm.py` | Step 4 核心代码（你要改的）|
| `data_processing/compute_sdf_3dfront.sh` | Step 4 运行脚本 |
| `data_processing/store_3dfuturefront_hjm.py` | Step 3 优化版（决定了 Step 4 的输入格式）|
| `data_processing/compute_train_chunks_vox_lowres_2x2_sem.py` | Step 5（Step 4 的下一步）|
| `data_processing/check_chunks.py` | 验证输出完整性 |
| `configs/specs_sem_vqvae.json` | Semantic VQ-VAE 训练配置 |
| `configs/specs_geo_vqvae_onestage.json` | Geometric VQ-VAE 训练配置 |
| `train_sdf/train.py` | VQ-VAE 训练入口 |
| `train_diffusion/train_ddp.py` | Diffusion 训练入口 |
| `train_sdf/dataloader/sdf_vox_loader.py` | 数据加载器（决定了训练时怎么读 Step 4 的输出）|
| `environment.yml` | 项目环境配置 |

---

*计划版本: v2（基于代码分析）*
*上次更新: 2026-05-27*
