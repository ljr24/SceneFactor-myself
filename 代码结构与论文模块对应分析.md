# SceneFactor 代码结构与论文模块对应分析

> 本文档将论文各部分映射到对应的代码文件，帮助理解"论文怎么说的"对应"代码怎么写"。

---

## 一、整体架构总览

### 论文与代码的对应

```
论文结构                              代码模块
─────────────────────────────────────────────────────
§3.1 分解式潜空间公式                   train_sdf/models/
  ├── 语义 VQ-VAE (§3.1.1)              ├── autoencoder_3d_vq_orig.py
  └── 几何 VQ-VAE (§3.1.2)              └── combined_model_3d_vq_orig_geo.py

§3.2 潜扩散模型                          train_diffusion/ + train_sdf/models/
  ├── 语义扩散 (§3.2.1)                  ├── diffusion.py, openai_model.py
  └── 几何扩散 (§3.2.2)                  └── context_encoding.py, attention.py

§3.3 滑动窗口外推                        inference_*.py + data_processing/
  ├── 语义块外推                          ├── inference_text_to_sem.py
  └── 几何块外推 + 外推编辑                └── inference_sem_to_geo_*.py

§4  数据预处理                            data_processing/
  ├── Steps 1-3: 3D-FRONT + 3D-FUTURE     ├── store_*, fix_*, compute_*
  ├── Step 4: SDF 体素化                   ├── compute_sdf_3dfront*.py
  ├── Step 5: 训练块采样                   ├── compute_train_chunks_vox*.py
  ├── Step 6: 推理块生成                   ├── compute_inference_chunks.py
  └── Steps 7-9: 标注文本生成              └── compute_captions*.py
```

### 关键概念映射

| 论文术语 | 代码中的命名 | 所在文件 |
|:---------|:-------------|:---------|
| 语义潜空间 $f_S$ | `lowres_latent`, `sem_latent` | `combined_model_3d_vq_orig.py` |
| 几何潜空间 $f_G$ | `latent`, `geo_latent` | `combined_model_3d_vq_orig_geo.py` |
| 语义 VQ-VAE 解码器 | `VQVAE` class | `autoencoder_3d_vq_orig.py` |
| 几何 VQ-VAE 解码器 | `VQVAE` class (同架构) | `combined_model_3d_vq_orig_geo.py` |
| 语义扩散模型 $\Psi_S$ | `UNetModel` + `GaussianDiffusion` | `openai_model.py`, `diffusion.py` |
| 几何扩散模型 $\Psi_G$ | `UNetModel` + `GaussianDiffusion` (同架构) | `openai_model.py`, `diffusion.py` |
| BERT 文本编码器 | `BERTEmbedder` | `context_encoding.py` |
| 卷积交叉注意力 | `SpatialTransformer`, `BasicTransformerBlock` | `attention.py` |
| Codebook 量化 | `VectorQuantizer` | `quantizer.py` |

---

## 二、数据处理模块 (`data_processing/`)

### 步骤 1-3 （已由团队完成）

| 步骤 | 输入 | 处理 | 输出 | 代码文件 |
|:-----|:-----|:-----|:-----|:---------|
| 1 | 3D-FUTURE 原始网格 | Manifold 修复非流形网格 | 修复后的 `.obj` | `store_manifold_3dfuture.py` |
| 2 | 修复后的 3D-FUTURE | 简化 + 补孔 | 优化后的 `.obj` | `fix_3dfuture_meshes.py` |
| 3 | 3D-FRONT + 3D-FUTURE | 组装场景 + 优化存储 | `SELECTED_FRONT_SCENES/` | `store_3dfuturefront*.py` |

**团队优化**：`store_3dfuturefront_hjm.py` 将重复存储的家具 mesh 改为存储 `*_trans.npy` 变换矩阵，运行时按 jid 加载+变换。

### 步骤 4：SDF 体素化（当前任务）

| 文件 | 说明 |
|:-----|:------|
| `compute_sdf_3dfront_hjm.py`（387 行） | **核心代码** — 团队优化版 |
| `compute_sdf_3dfront.py`（原版） | 原版，直接加载所有 mesh |
| `compute_sdf_3dfront.sh` | 运行脚本（Slurm/本地） |

**输入**:
- `FRONT3DMESHESMANIFOLD/<scene_id>/` — 场景目录（`scene.obj`, `furniture_jids.json`, `furniture_points.json`, `scene_points.json`, `*_trans.npy`, `*_furniture.obj`）
- `FUTURE3DMODEL/<jid>/` — 3D-FUTURE 模型（`raw_model.obj` 或 `raw_model_fixed.obj`）

**处理流程**（对应论文 §4 Data Preparation）:
```
for each scene:
  1. 加载 scene.obj → scene_mesh
  2. load_scene_furniture() → 按 jid 从 FUTURE3DMODEL 加载家具 + 从 *_trans.npy 获取变换矩阵 → 变换到位
  3. 合并 scene_mesh + furniture_mesh
  4. 计算包围盒 → 按 3.8m 步长切分 chunk 网格
  5. for each chunk:
     a. 将 mesh 平移到中心并归一化到 [-1, 1]
     b. mesh2sdf.core.compute() → 90³ SDF 张量
     c. 截断 SDF（>0.08 的设为 0.08）
     d. 用 cKDTree 将家具点云映射到体素 → semantic ID + instance ID + canonic 坐标
     e. 保存 .npy + .json
```

**输出** (`chunked_data_lowres/<scene_id>/`):
| 文件 | 内容 | 形状 | 类型 |
|:-----|:-----|:-----|:-----|
| `{x}_0_{z}.npy` | SDF 张量 | 90×90×90 | float16 |
| `{x}_0_{z}_semantic.npy` | 语义类别 ID | 90×90×90 | int16 |
| `{x}_0_{z}_instance.npy` | 实例 ID | 90×90×90 | int16 |
| `{x}_0_{z}_canonic.npy` | 正则化坐标 | 90×90×90×3 | int16 |
| `{x}_0_{z}.json` | 元数据 | — | JSON |

**关注点**:
- `compute_sdf_3dfront_hjm.py` line 239: `if xyz_index[1] != 0: continue` — **只处理 y=0 层的 chunk**，即 2D 切片。这意味着场景按水平（XZ）平面切格，Y（高度）维度在单层内。
- `compute_sdf_3dfront.py` 则没有这个限制，处理所有层。

### 步骤 5：训练块采样

| 文件 | 说明 |
|:-----|:------|
| `compute_train_chunks_vox_lowres_2x2_sem.py` | 从 90³ 的 SDF chunk 中采样出 64³ 的训练 subchunk |

**关键逻辑**: 只选有家具的 chunk 作为采样中心，然后加上 8 邻域信息。这对应论文中"训练时只选有物体的区域"的策略。

### 步骤 6-9：推理块 + 标注文本

| 文件 | 说明 |
|:-----|:------|
| `compute_inference_chunks.py` | 为推理生成块 |
| `compute_captions.py` | 简单合成标注文本 |
| `compute_captions_spatial.py` | 带空间关系的标注文本 |
| `compute_all_captions_qwen_train.py` | Qwen1.4 精炼标注文本 |

---

## 三、VQ-VAE 训练模块 (`train_sdf/`)

### 3.1 训练入口

| 文件 | 作用 |
|:-----|:------|
| `train.py`（119 行） | PyTorch Lightning 训练入口，自动选择 Sem/Geo VQ-VAE |
| `train_modulation.sh` | 启动脚本 |

**启动流程**:
```bash
cd train_sdf
python train.py -e <exp_dir> -b <batch_size> -w <workers>
```
其中 `exp_dir/specs.json` 是配置文件（参见 `configs/`）。

**`train.py` 关键逻辑**:
- `specs["Factor"] = "sem"` → 用 `VoxLoaderSem` + `CombinedModel3DVQOrig`
- `specs["Factor"] = "geo"` → 用 `VoxLoaderGeo` + `CombinedModel3DVQOrigGeo`
- `specs["training_task"] = "modulation"` → 只训 VQ-VAE
- `specs["training_task"] = "combined"` → 同时训 VQ-VAE + 扩散模型

### 3.2 语义 VQ-VAE（对应论文 §3.1.1）

| 代码文件 | 类/函数 | 对应论文 |
|:---------|:--------|:---------|
| `models/combined_model_3d_vq_orig.py` | `CombinedModel3DVQOrig` | 语义 VQ-VAE + 扩散训练器 |
| `models/autoencoder_3d_vq_orig.py` | `VQVAE` | 3D Encoder-Decoder + Codebook |
| `models/vqvae_modules.py` | `Encoder`, `Decoder` | 3D 卷积编解码器 |
| `models/quantizer.py` | `VectorQuantizer` | Codebook 量化 |
| `dataloader/sdf_vox_loader.py` | `VoxLoaderSem` | 语义数据加载 |

**数据流**:
```
VoxLoaderSem.__getitem__():
  *.npy 中的 {chunk}_semantic.npy → 随机裁切 + 数据增广 → one-hot 编码(10类) → [10, 32, 16, 32]

CombinedModel3DVQOrig.train_modulation():
  onehot[10, D, H, W] → VQVAE(vox) → (pred_sem, emb_loss)
  VQVAE.encoder → latent[1, 8, 4, 8] → VectorQuantizer → VQVAE.decoder → pred_sem
  loss = CrossEntropy(pred_sem, gt_sem) + emb_loss
```

**配置** (`configs/specs_sem_vqvae.json`):
```json
{
  "resolution": 16,       // 输入语义块尺寸
  "in_channels": 10,      // 10类 one-hot
  "ch": 16,               // 初始通道数
  "ch_mult": [2, 4],      // 通道倍增
  "z_channels": 1,        // 潜通道数
  "n_embed": 8192,        // Codebook 大小
  "embed_dim": 1          // 潜维度
}
```

### 3.3 几何 VQ-VAE（对应论文 §3.1.2）

| 代码文件 | 类/函数 | 对应论文 |
|:---------|:--------|:---------|
| `models/combined_model_3d_vq_orig_geo.py` | `CombinedModel3DVQOrigGeo` | 几何 VQ-VAE + 扩散训练器 |
| `models/autoencoder_3d_vq_orig.py` | `VQVAE`（复用） | 同一架构 |
| `dataloader/sdf_vox_loader.py` | `VoxLoaderGeo` | 几何数据加载 |

**数据流**:
```
VoxLoaderGeo.__getitem__():
  {chunk}.npy → 随机裁切(SDF范围) + 数据增广 → SDF截断/归一化 → [1, 128, 64, 128]

CombinedModel3DVQOrigGeo.train_modulation():
  SDF[1, 128, 64, 128] → VQVAE → (pred_geo, emb_loss)
  loss = L1(pred_geo, gt_geo) + emb_loss
```

**配置** (`configs/specs_geo_vqvae_onestage.json`):
```json
{
  "resolution": 64,
  "in_channels": 1,
  "ch": 32,
  "ch_mult": [1, 2, 4],
  "z_channels": 1,
  "n_embed": 32768,
  "embed_dim": 1
}
```

### 3.4 数据加载器

| 文件 | 类 | 用途 |
|:-----|:---|:------|
| `dataloader/base.py` | `Dataset` | 基类，提供 `get_instance_filenames_scene_vox_list()` |
| `dataloader/sdf_vox_loader.py` | `VoxLoaderGeo` | 几何 SDF 加载 |
| `dataloader/sdf_vox_loader.py` | `VoxLoaderSem` | 语义 one-hot 加载 |
| `dataloader/cap_vox_loader.py` | `CapVoxloader` | 带标注文本的加载 |
| `dataloader/cap_vox_loader.py` | `Roomloader` | 整房间加载 |
| `dataloader/test_loader.py` | 测试用加载器 | 评估指标 |

---

## 四、扩散模型 (`train_diffusion/` + `train_sdf/models/`)

### 4.1 语义扩散（对应论文 §3.3 公式 6）

| 代码文件 | 类/函数 | 对应论文 |
|:---------|:--------|:---------|
| `train_diffusion/train_ddp.py` | DDP 训练入口 | §3.3 语义 LDM 训练 |
| `train_diffusion/diffusion.py` | `GaussianDiffusion` | DDPM 前向/反向过程 |
| `train_diffusion/openai_model.py` | `UNetModel` | 3D U-Net 主干 |
| `train_diffusion/context_encoding.py` | `BERTEmbedder` | BERT 文本编码 → 条件注入 |
| `train_diffusion/attention.py` | `SpatialTransformer` | 含交叉注意力 |
| `train_diffusion/x_transformer.py` | `Encoder`, `Decoder` | Transformer 模块 |
| `train_diffusion/model.py` | `Dataset`, 模型组装 | 数据和模型准备 |
| `configs/specs_sem_diff.json` | 语义扩散配置 | 超参数 |

**注意力机制**（对应论文 §3.3 卷积交叉注意力）:
```
text → BERTEmbedder → 1280-dim → UNetModel cross-attention
UNetModel 中的 attention 层: SpatialTransformer
  → BasicTransformerBlock → LinearCrossAttention(text_features, latent_grid)
```

**配置** (`configs/specs_sem_diff.json`):
```json
{
  "model_channels": 96,
  "channel_mult": [1, 2, 4],
  "num_res_blocks": 1,
  "attention_resolutions": [4, 2, 1]
}
```

### 4.2 几何扩散（对应论文 §3.3 公式 7）

| 代码文件 | 说明 |
|:---------|:------|
| `train_diffusion/train_ddp.py` | 同语义扩散入口，配置不同 |
| `configs/specs_geo_diff_onestage.json` | 单阶段几何扩散 |
| `configs/specs_geo_diff_stage1.json` | 两阶段-阶段1 |
| `configs/specs_geo_diff_stage2.json` | 两阶段-阶段2 |

**与语义扩散的唯一区别**（设计如此）:
- 条件从 `text` 变为 `semantic latent map`
- `spatial cross-attention` 卷积窗口 3×3×3（而非逐点）

### 4.3 训练脚本

| 脚本 | 用途 | 备注 |
|:-----|:-----|:------|
| `train_modulation.sh` | 训练 VQ-VAE | `batch_size=32`, `mem=200gb`, 1 GPU |
| `train_diffusion_ddp.sh` | 训练扩散模型 | `batch_size=8/16`, `mem=160gb`, 8 GPU DDP |

---

## 五、推理模块 (`train_sdf/`)

### 5.1 逐块生成

| 推理脚本 | 对应的代码 | 功能 |
|:---------|:-----------|:-----|
| `inference_separate_sem.sh` | `inference_separate_sem.py` | 从文本生成语义块 |
| `inference_separate_geo.sh` | `inference_separate_geo.py` | 从语义块生成几何块 |
| `inference_separate_geo_stage2.sh` | `inference_separate_geo_stage2.py` | 精炼几何块 |

### 5.2 大规模场景生成（对应论文 §3.4）

| 推理脚本 | 对应的代码 | 功能 |
|:---------|:-----------|:-----|
| `inference_text_to_sem.sh` | `inference_text_to_sem.py` | 文本→语义场景（滑动窗口）|
| `inference_sem_to_geo_overlap_stage1_onestage.sh` | `inference_sem_to_geo_overlap_stage1_onestage.py` | 语义→几何（重叠窗）|
| `inference_sem_to_geo_nooverlap_stage2.sh` | `inference_sem_to_geo_nooverlap_stage2.py` | 精炼几何 |

### 5.3 编辑（对应论文 §3.4 编辑实验）

| 推理脚本 | 对应的代码 | 功能 |
|:---------|:-----------|:-----|
| `inference_sem_to_geo_overlap_edit_stage1_onestage.sh` | `.py` | 编辑：添加/移除/替换/移动/缩放 |
| `inference_sem_to_geo_overlap_edit_stage2.sh` | `.py` | 编辑精炼 |
| `inference_sem_to_geo_nooverlap_edit_stage2.sh` | `.py` | 编辑精炼（无重叠）|

---

## 六、模型架构详解

### 6.1 VQ-VAE 3D 编解码器

**文件**: `train_sdf/models/vqvae_modules.py`

```
Encoder:                              Decoder:
Input [C, D, H, W]                    Input [Z, D', H', W']
  └── Conv3d + GroupNorm + Swish        └── Conv3d + GroupNorm + Swish
  └── DownBlock × N (stride=2)          └── UpBlock × N (stride=2)
  └── MidBlock (Attn)                   └── MidBlock (Attn)
  └── Conv3d → quant_conv             └── Conv3d → post_quant_conv
  └── output latent [Z, D', H', W']    └── output [C, D, H, W]
```

### 6.2 3D U-Net（扩散主干）

**文件**: `train_sdf/models/openai_model.py`, `train_diffusion/openai_model.py`

基于 OpenAI GLIDE 的 3D 适配版：
- 3D 卷积替代 2D 卷积
- 含时间嵌入（timestep embedding）+ 条件嵌入
- 跨层跳跃连接（skip connections）

### 6.3 注意力机制

**文件**: `train_sdf/models/attention.py`, `train_diffusion/attention.py`

| 组件 | 文件 | 作用 |
|:-----|:-----|:------|
| `SpatialTransformer` | `attention.py` | 空间维度上的 transformer |
| `BasicTransformerBlock` | `attention.py` | Self-Attn + Cross-Attn + FF |
| `LinearCrossAttention` | `attention.py` | 线性交叉注意力（文本条件用）|
| `ConvCrossAttention` | `attention.py` | 卷积交叉注意力（语义条件用）|

### 6.4 Codebook 量化

**文件**: `train_sdf/models/quantizer.py`

标准 VQ-VAE 量化：
- 查找最近的 codebook 向量 → one-hot → 加权求和
- 含直通估计（straight-through estimator）用于梯度传播
- 计算 codebook 对齐损失（commitment loss）

---

## 七、配置文件速查 (`configs/`)

| 文件 | 用途 | 关键参数 |
|:-----|:------|:---------|
| `specs_sem_vqvae.json` | 语义 VQ-VAE | in_ch=10, res=16, ch=16, n_embed=8192 |
| `specs_geo_vqvae_onestage.json` | 几何 VQ-VAE（单阶段）| in_ch=1, res=64, ch=32, n_embed=32768 |
| `specs_geo_vqvae_stage1.json` | 几何 VQ-VAE（阶段1） | res=128, ch_mult=[1,2,2,4], n_embed=32768 |
| `specs_geo_vqvae_stage2.json` | 几何 VQ-VAE（阶段2） | res=128, ch_mult=[1,2,2], n_embed=8192 |
| `specs_sem_diff.json` | 语义扩散 | model_ch=96, ch_mult=[1,2,4] |
| `specs_geo_diff_onestage.json` | 几何扩散（单阶段） | model_ch=96, ch_mult=[1,2,4] |
| `specs_geo_diff_stage1.json` | 几何扩散（阶段1） | model_ch=128, ch_mult=[1,2,4,4] |
| `specs_geo_diff_stage2.json` | 几何扩散（阶段2） | model_ch=96, ch_mult=[1,2,4,4] |
| `specs_sem_infer.json` | 语义推理 | 需指定 VQ-VAE + Diffusion checkpoint 路径 |
| `specs_geo_infer_stage1_onestage.json` | 几何推理 | 需指定 VQ-VAE + Diffusion checkpoint 路径 |
| `specs_geo_infer_stage2.json` | 几何推理精炼 | 需指定 stage2 的 checkpoints |

---

## 八、多方案对比：One-stage vs Two-stage

| 维度 | One-stage | Two-stage |
|:-----|:----------|:----------|
| 几何 VQ-VAE 配置 | `specs_geo_vqvae_onestage.json` | `specs_geo_vqvae_stage1.json` + `stage2.json` |
| 几何扩散配置 | `specs_geo_diff_onestage.json` | `specs_geo_diff_stage1.json` + `stage2.json` |
| SDF 分辨率 | 64³ | Stage1: 128³ → Stage2: 128³ |
| Codebook 大小 | 32768 | Stage1: 32768, Stage2: 8192 |
| 对应论文 | 简化版 | 完整版 |
| 检查点文件 | 2 VQ + 2 Diff | 3 VQ + 4 Diff |

---

## 九、关键文件速查索引

```
SceneFactor/
├── README.md                            ← 项目总文档（含团队中文注释）
├── environment.yml                      ← Conda 环境配置
├── data_processing/                     ← 数据预处理（当前工作区域）
│   ├── compute_sdf_3dfront_hjm.py       ← Step 4 核心（团队优化版）
│   ├── compute_sdf_3dfront.py           ← Step 4 原版
│   └── compute_train_chunks_vox_*.py    ← Step 5 训练块采样
├── train_sdf/                           ← VQ-VAE 训练
│   ├── train.py                         ← 训练入口
│   ├── models/
│   │   ├── autoencoder_3d_vq_orig.py    ← VQVAE 模型
│   │   ├── combined_model_3d_vq_orig.py ← 语义 VQ-VAE+Diffusion 训练器
│   │   ├── combined_model_3d_vq_orig_geo.py ← 几何 VQ-VAE+Diffusion 训练器
│   │   ├── vqvae_modules.py             ← Encoder/Decoder 模块
│   │   ├── quantizer.py                 ← VQ Codebook 量化
│   │   ├── openai_model.py              ← 3D U-Net 扩散主干
│   │   ├── diffusion.py                 ← 扩散过程
│   │   ├── context_encoding.py          ← BERT 条件编码
│   │   └── attention.py                 ← 空间+交叉注意力
│   └── dataloader/
│       ├── sdf_vox_loader.py            ← 几何/语义数据加载
│       └── cap_vox_loader.py            ← 带标注文本的加载
├── train_diffusion/                     ← 扩散模型训练（DDP）
│   ├── train_ddp.py                     ← DDP 训练入口
│   ├── diffusion.py                     ← 扩散过程
│   ├── openai_model.py                  ← 3D U-Net
│   ├── context_encoding.py              ← 条件编码
│   └── attention.py                     ← 注意力
├── configs/                             ← 所有配置文件
└── train_*.sh, inference_*.sh           ← 运行脚本
```
